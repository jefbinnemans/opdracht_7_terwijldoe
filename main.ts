let index = 9
while (index != 0) {
    basic.showNumber(index)
    index += -1
    basic.pause(600)
}
while (true) {
    basic.showIcon(IconNames.Butterfly)
    basic.showIcon(IconNames.Tortoise)
    basic.clearScreen()
}
